from sdot.bindings.construct import construct, args_to_obj
from sdot.symbolic.Expr import Expr

args = args_to_obj( ARGLIST )

print( Expr.list_from_compact_repr( args.symbolic_func ) )
oue()

construct( Environment, VariantDir, Configure, ARGLIST, "integration", [], [
    'build/integration_bindings.cpp',
] )



